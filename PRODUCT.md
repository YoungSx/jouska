# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

运维/平台工程师，同时是 jouska 反向代理的部署者与配置者。通常是一到三人的小团队，其中至少一人有 Cloudflare 账号的写权限。

到达面板时的处境有三种，UI 必须分别认得出来：

- **首次部署刚结束**：数据库是空的，没有账号，没有路由。此人刚跑完 `npm run cf:setup`，想知道"接下来干什么"。
- **改一条配置**：面板已在用，此人要改某条路由的上游或超时，然后发布。多数会话属于这一类，且往往在告警之后、时间压力之下发生。
- **事后追查**：某次变更之后代理行为变了，此人要看审计日志找出谁在什么时候改了什么。

## Product Purpose

jouska 是 Hono 上的反向代理中间件，运行在 Cloudflare Workers。管理面板是它的配置平面：把声明式路由表存进 D1，编译成一份文档写入 KV，反代在热路径上读这份 KV。

面板存在的理由是：路由表直接决定生产流量去哪。手改 KV 没有校验、没有审计、没有二次确认，一个错字就能把代理变成内网探测器。面板把这件事变成一个有闸门的流程。

成功的定义是：改一条路由到发布上线，全程不需要打开终端，而且每一次危险的改动都被拦下来问过一次。

## Positioning

面板与反代共用同一份 `jouska` 库的代码来做校验与遮蔽检测。

- 发布前的校验走 `configSchema`（反代运行时用的同一个 zod schema），不是第二套近似实现。
- 遮蔽检测（`shadow.ts`）不重新实现匹配逻辑，而是从每条路由自身的模式生成探测 URL，交给库的 `matchUrl` 判定。所以"这条路由永远收不到流量"这个警告，是由跑在生产上的同一段代码证明的，不会与实际行为漂移。

这一点邻近产品无法照抄：它要求配置平面和数据平面共享一个库。

## Operating Context

单 Worker 单源站：面板与它的 API 同源部署（`run_worker_first: ["/api/*"]`），所以 CORS 与 Cookie 不会漂移。

数据流向是单向的：

```
操作者 → 面板 UI → /api/* → D1（草稿：routes / settings / users / audit_log）
                              ↓ 发布（唯一一次 KV 写）
                           CONFIG_KV['routes'] → 反向代理热路径读取
```

关键后果：**D1 里的改动不会立刻生效**。改路由是改草稿；只有"发布"这个动作才影响生产流量。UI 必须一直让这个区别可见，否则操作者会以为保存即上线。

发布是这个 Worker 唯一的 KV 写入，因为免费额度的每日写次数不该被浪费。

角色两种：`admin`（读写、可发布）与 `viewer`（只读）。写操作在各自 handler 上逐个把关，不是按前缀一刀切，所以 viewer 保留全部读取能力。

首次运行走 bootstrap：`users` 表为空时，第一个调用者创建初始 admin，此后永久关闭（行数检查 + 唯一索引共同关掉竞态）。

密码丢了或账号锁死走**带外恢复**：有数据库权限的人在 `settings` 表里开一个一次性令牌窗口（CI 的 `admin-reset` 工作流，或直接写一行 SQL），操作者在登录页用「账号 + 令牌 + 新密码」重置。令牌用过即失效，可以钉到某个账号上，所以泄露的令牌不能被转去重置别的 admin。服务端刻意把「没开窗口 / 令牌不对 / 已过期 / 账号名不符」压成同一个 `recovery_unavailable` —— 前端不得替它区分。

域名发现是只读查询：面板用一个**只读**的 Cloudflare API token 读出账号里真正能打到反代 Worker 的 hostname（workers.dev 子域、自定义域、zone route），再和路由表交叉比对，回答操作者写 `match.host` 时的两个问题——哪个域名没有路由接管，哪条路由指着一个还没绑定的域名。凭据缺失不是错误：那是受支持的部署，界面解释而不是报警。

## Capabilities and Constraints

已有能力：

- 路由 CRUD、顺序调整（顺序即优先级，首个匹配胜出）、启用/停用。
- 表级 `defaults` 块，逐字段填补空缺。
- 发布预览：编译 + 校验 + 遮蔽检测 + 整站镜像提示 + 危险开关分类，不写 KV。
- 发布：三重闸门（必须编译通过、危险开关需显式 `confirm`、全部进审计日志），一次发布恰好一次 KV 写，`meta` 带 revision/操作者/备注。
- 发布历史与回滚：每次发布存一份快照（含 `defaults` 与全部启用路由，滚动保留 50 版），时间轴可按 revision 浏览、任选两版做服务端字段级 diff；回滚把目标快照恢复进草稿并复用同一条发布管道——它是发布成新 revision，不是倒带计数，`rollbackOf` 记录出处；目标快照先过 schema 校验、再重过全部发布闸门。历史功能之前的旧发布以审计日志回填为「无快照」条目，能看、不能比。
- 审计日志，最多 200 条每页。
- 登录、登出、会话；连续 5 次失败锁 15 分钟。
- 带外密码恢复：一次性令牌，可钉到账号，用过即废，走审计（`auth.recover`）。
- 域名发现：只读列出账号里绑定到反代的 hostname，与路由表交叉比对；60 秒 isolate 内缓存，不落盘。

技术约束：

- 部署目标是 Cloudflare Workers 的静态资源 + 单 Worker，产物必须是纯静态文件。
- `assets.not_found_handling: "single-page-application"`，所以客户端路由可用。
- 路由定义单条上限 64 KiB，`defaults` 同；发布备注 500 字符；密码 12–1024 字符。
- 存量数据可能损坏：D1 里的 JSON 列若解析失败，读路径必须降级为"具名的问题"，不能 500——否则操作者被锁在他唯一能修好它的那个屏幕外面。

明确的缺口（真实存在的表与字段，但零 UI，未来工作要接上，不许假装已有）：

- `users` 表已有 `role` / `disabled` / `failed_attempts` / `locked_until`，但面板没有任何用户管理界面：加号、改角色、停用、解锁、踢会话、改密码都做不到。唯一的建号途径是首次 bootstrap。
- 路由级运行时可观测（请求量、错误率、p95、上游健康）的数据源尚不存在，需要 Analytics Engine 之类的接入。

## Brand Commitments

- 产品名 `jouska`，全小写。
- 界面语言中文（zh-CN）。文案集中在一个 messages 模块，当前只有 zh-CN，未来加语言不改组件。
- 语气：说清后果，不吓人。空状态是"还没开始"，不是"配置有错"——刚部署完就说配置有问题只会让人以为装坏了。危险开关不禁止（每一个都有正当用途），只让手指变重。

## Evidence on Hand

真实存在、可直接引用：

- `README.md`：完整的路由选项表（20+ 字段及默认值）、guards、body rewrite 文档。
- `packages/jouska/src/config.ts`：权威 zod schema，含归一化规则与安全注释。
- `workers/admin-panel/src/danger.ts`：8 条危险字段分类，每条带 level 与人类可读的 reason。
- `workers/admin-panel/src/shadow.ts`：遮蔽检测，产出 `shadowedId` / `byId` / `probe`。
- `workers/admin-panel/src/mirror.ts`：整站镜像提示，产出 `routeId` / `upstream`；判据只覆盖整站路由，且是提示不是错误。
- `workers/admin-panel/migrations/`：`0001_init.sql`、`0002_tables.sql`、`0003_revisions.sql` 的真实表结构。
- `workers/admin-panel/src/cloudflare.ts`：域名发现的三个来源与它们各自的失败模式。
- `workers/admin-panel/src/recovery.ts`：恢复令牌的窗口、过期与一次性消费语义。

不存在，不许编造：性能基准数字、用户数量、客户案例、可用性承诺、任何路由的实时流量数据。

## Product Principles

1. **草稿与生产是两件事，UI 永远让这个区别可见。** 保存不等于上线；发布是唯一改变生产流量的动作。
2. **危险不禁止，只让手指变重。** 每个危险开关都有正当用途，面板的职责是让操作者在按下去之前确实看见后果。
3. **校验用生产的那份代码，不用第二个意见。** 任何"这样不对"的判断都必须由反代实际运行的同一段逻辑得出，否则警告会与真实行为漂移。
4. **空不是坏。** 空路由表、空审计、首次部署都是正常状态，要引导下一步，不要报错。
5. **损坏的数据必须能在界面里被看见并修好。** 读路径遇到坏数据降级为具名问题，绝不把操作者挡在修复界面之外。

## Accessibility & Inclusion

面板可能在告警中、深夜、单手操作下使用。键盘可达与焦点可见是硬要求，不是加分项：全部交互控件必须可 Tab 到达、焦点环可见、Dialog 有焦点陷阱与 Esc 关闭。这也是选用 Base UI 的直接理由。
